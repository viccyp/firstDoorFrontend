@using firstDoorFrontEnd.Models;
@rendermode InteractiveServer

@if (SavedJob == null)
{
    <EditForm FormName="AddJobNote"></EditForm>
}

@code {
    [Parameter]
    public SavedJob? SavedJob { get; set; }
}